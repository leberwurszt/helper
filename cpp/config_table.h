#pragma once

#include <iostream>
#include <map>
#include <string>

class ConfigTable {
    private:
        std::map<uint16_t, std::string> configNameMap;
        std::map<uint16_t, std::string> configValueMap;
    public:
        
};